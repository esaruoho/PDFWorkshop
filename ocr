#!/bin/bash
# OCR a single PDF or all PDFs in a folder.
# Usage: ./ocr file.pdf
#        ./ocr /path/to/folder
#        ./ocr file.pdf --output /path/to/results
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
node "$SCRIPT_DIR/batch-ocr.mjs" "$@"
